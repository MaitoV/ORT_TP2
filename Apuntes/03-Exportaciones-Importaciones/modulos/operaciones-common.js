/* EXPORT COMMONJS */
const suma = (a,b) => a + b;
const resta = (a,b) => a - b;
const mult = (a,b) => a * b;
const div = (a,b) => a / b;

module. exports = {
    suma, // es lo mismo que suma: suma. Una simplifacion de js cuando la clave coincide con el nombre de la variable de la que se obtene el dato, se coloca su nombre 1 sola vez
}

